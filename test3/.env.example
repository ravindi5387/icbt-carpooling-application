# Use true while building/demoing the frontend before the backend is connected.
VITE_DEMO_MODE=true

# Replace this with the actual Suhitha backend URL when integration is ready.
VITE_API_URL=http://localhost:5000/api

# Optional WebSocket endpoint for near-real-time chat.
VITE_WS_URL=ws://localhost:5000
